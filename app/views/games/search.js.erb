<%# For some reason js.erb doesn't work so I did it like that%>
  var genres = document.getElementById('genres');
  var games = document.getElementById('games')

  genres.innerHTML = "<%= j render 'games/genres' %>";
